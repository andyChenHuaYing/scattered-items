Type Information

    The need for RTTI(Runtime type information)
        List<Shape> shapes = Arrays.asList(new Circle(), new Triangle(), new Square());

    The Class object
        Class.forName(String fullClassName);
            The argument must be class's package name + class name and the class file of this Class must exist;
            JVM:The class loader subsystem can actually comprise a chain of class loaders, but there's only one
            primordial class loader.
        getClass();